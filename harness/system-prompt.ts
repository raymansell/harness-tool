// Agent prompts now live in agents.ts (one per agent). This file just holds a
// sample task to try.
//
// A multi-area escalation request: try in Supervised mode to trigger the four
// phases: decompose objective into a structured plan → dispatch sub-agents in
// parallel → fan in → synthesize.
export const SAMPLE_TASK = `Customer cus_88121: double-charged, the export feature is broken in Safari, and they want 50-seat pricing.`;
