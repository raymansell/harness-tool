// Agent prompts now live in agents.ts (one per agent). This file just holds a
// sample task to try.
//
// A refund request. The `issueRefund` tool now requires human approval.
export const SAMPLE_TASK = `Customer cus_88121 was charged twice, refund the duplicate.`;
